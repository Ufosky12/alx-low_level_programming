#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	/* Program to print single digits from 0 - 9 using printf */

	int nums;

	for (nums = 0; nums < 10; nums++)
		printf("%d", nums);
	printf("\n");
	return (0);
}
